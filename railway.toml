[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m streamlit run main.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
